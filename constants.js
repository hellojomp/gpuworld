// Shared constants. This file is also read by .githooks/pre-commit (via a
// plain-text grep, since it's a bash script), so keep MAX_WORD_COUNT on its
// own line in this exact `NAME = <number>;` form.
const MAX_WORD_COUNT = 1000;
